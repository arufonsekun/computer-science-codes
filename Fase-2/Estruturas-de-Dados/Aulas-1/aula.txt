tipos de variáveis{
        escalar: só armazena um valor por vez (int, float, char) - tipos primitivos;
        vetorial: armazena diversos valores com o mesmo nome (vetor e matriz);
}

vetores:
matrizes:
structs: variável composta;

NUNCA COLOCAR \N NO SCANF()

COMANDOS QUE EU NÃO CONHECIA
//Imprime uma string com 20 caracteres
printf("20s", string);
getcha(); -> limpa o buffer de entrada
