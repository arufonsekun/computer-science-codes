binaria - arvore head{
         - min;
         - max;
         - usado para fila de prioridade;
         - algoritmo de ordenação;
         - left <- k*2;
         - right <- k*2+1;
         - parent floor(k/2);
         - [0|1|2|3|4|5|6];
}

        MIN HEAP
             3
      30          43
  50     75   80      70
100
