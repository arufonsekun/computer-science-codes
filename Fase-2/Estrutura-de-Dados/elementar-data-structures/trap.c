#include <stdio.h>

int main(){
  int x = 0;
  int *pointer = &x;
  vector = (int*) malloc(sizeof(int) * 10);

  /*for (unsigned int i = 0; i < 100; i++){
    printf("%d\n", &pointer);
    pointer++;
  }*/
  return 0;
}
